{
  "name": "volunteer-organizer",
  "version": "1.0.0",
  "description": "Full-stack volunteer management application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-deps": "cd server && npm install && cd ../client && npm install",
    "setup": "npm run install-deps"
  },
  "keywords": ["volunteer", "management", "mern", "react", "node"],
  "author": "Volunteer Organizer Team",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
